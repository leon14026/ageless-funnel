# Dependencies
node_modules/

# Secrets — NEVER commit these. (config.js is intentionally NOT ignored: it holds
# only PUBLIC values. True secrets live as Supabase Edge Function secrets.)
.env
.env.*
*.key

# Database dumps from scripts/backup-db.sh. These hold customer emails, phone
# numbers, addresses and payment records — never commit them.
backups/

# Security notes. A completed audit names this site's weaknesses and the controls
# that cover them; that belongs in private notes, not in a repo that may later be
# shared or made public.
website_cybersecurity_audit_checklist.md

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.log

# Python tooling that may sit beside image assets
__pycache__/
*.py[cod]
